services:
  backend:
    build:
      context: ./src
    container_name: backend
    ports:
      - "3000:3000"
    env_file:
      - ./src/backend/.env  
    depends_on:
      - postgres

  postgres:
    image: postgres:17
    container_name: postgres
    env_file:
      - ./src/backend/.env  
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

